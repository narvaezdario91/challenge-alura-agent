## 1. Instalación y Configuración de uv

- [x] 1.1 Ejecutar `uv init` en la raíz del proyecto para generar `pyproject.toml`
- [x] 1.2 Añadir dependencias base del agente (por ejemplo: `langgraph`, `langchain`) usando el comando `uv add`

## 2. Documentación

- [x] 2.1 Actualizar el `README.md` añadiendo instrucciones sobre cómo utilizar `uv` (ej. `uv sync`, `uv run`) para levantar el proyecto
